import React from "react";

import { Typography } from "@material-ui/core";

const Today = () => {
  return (
    <div>
      <Typography component="h1">Today</Typography>
    </div>
  );
};

export default Today;
